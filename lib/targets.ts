/**
 * Platform targets shown in the UI. One constant per target so every page
 * quotes the same number — the audit found 98% and "<3%" hardcoded on
 * different pages for the same idea.
 */
export const PAYMENT_SUCCESS_TARGET_PCT = 98
